import React from 'react';
import styled from 'styled-components';

export function NavigationLink({ name, url, icon }) {
  return <StyledNavigationLink></StyledNavigationLink>;
}

const StyledNavigationLink = styled.a``;
